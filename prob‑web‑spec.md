# prob‑web‑spec.md
> 概率论与数理统计交互式可视化教学网站｜完整规格说明书
> 用途：直接复制片段到大模型，分批次生成网站HTML代码；**禁止一次性全部丢入，按首页→章节1→章节2…顺序生成**
> 技术栈：静态网页，无后端；全部依赖CDN；TailwindCSS + KaTeX(公式) + ECharts(2D图表) + Plotly.js(3D/等高线)
> 目标用户：本科课堂、学生自学；兼顾概念理解 + 应试考点提示

---

## 全局UI&技术规范（所有页面共用）
```
【UI风格】
- 主题：简洁科研教学风，浅色模式为主；主色 #2563eb，强调色 #1d4ed8；应试提示背景 #fef9c3
- 布局：左侧固定侧边导航栏，PC端优先；卡片圆角，合理留白；卡片之间垂直间距24px
- 卡片统一结构：
  1. 卡片标题
  2. 📖知识点文本（简短，教材精简版）
  3. 📐公式折叠面板：点击展开/收起，KaTeX渲染LaTeX
  4. 🎛️可视化区域：图表容器 + 滑块、下拉选择、按钮、重置按钮
  5. 💡应试提示：浅黄色背景，标注考试高频点、易错坑
- 页面底部：2‑3道单选自测小题
- 所有JS写在页面内部<script>标签，完整可直接运行，不引入本地js文件
- 外部CDN列表，每个html头部必须引入：
1. TailwindCSS
2. KaTeX css & js
3. ECharts js
4. Plotly.js js
```

```html
<!-- CDN头部模板，每个HTML都复制 -->
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.4/dist/tailwind.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.css">
<script src="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/echarts@5.4/dist/echarts.min.js"></script>
<script src="https://cdn.plot.ly/plotly-2.32.0.min.js"></script>
```

【侧边导航栏固定内容】
```
导航标题：概率论与数理统计可视化平台
导航链接列表：
1. 首页（index.html）
2. 随机事件及其概率（chapter1.html）
3. 一维随机变量及其分布（chapter2.html）
4. 二维随机变量及其分布（chapter3.html）
5. 随机变量的数字特征（chapter4.html）
6. 大数定律及中心极限定理（chapter5.html）
7. 样本及抽样分布（chapter6.html）
8. 参数估计（chapter7.html）
9. 假设检验（chapter8.html）
- 当前所在页面导航项高亮蓝色
```

【网站目录结构】
```
prob‑visual‑web/
├── index.html               # 首页：课程简介、目录导航、使用说明
├── pages/
│   ├── chapter1.html        # 随机事件及其概率
│   ├── chapter2.html        # 一维随机变量及其分布
│   ├── chapter3.html        # 二维随机变量及其分布
│   ├── chapter4.html        # 随机变量的数字特征
│   ├── chapter5.html        # 大数定律及中心极限定理
│   ├── chapter6.html        # 样本及抽样分布
│   ├── chapter7.html        # 参数估计
│   └── chapter8.html        # 假设检验
├── assets/
│   ├── css/ main.css        # 公共样式
│   ├── js/
│   │    ├── common.js       # 公共工具函数
│   │    ├── echarts.min.js
│   │    └── plotly‑min.js
│   └── formulas/            # 公式文本、markdown片段
└── README.md                # 部署说明
```

---

# 首页 index.html 规格
## 知识点文本
> 《概率论与数理统计交互式可视化平台》
> 课程分为两大模块：
> 1. 概率论：已知概率模型，计算随机事件发生的概率；研究随机变量、分布、数字特征。
> 2. 数理统计：由样本数据反推总体信息；参数估计、假设检验。
> 使用说明：
> 1. 左侧导航切换章节；
> 2. 拖动滑块修改参数，观察图表实时变化；
> 3. 折叠面板查看考试公式；
> 4. 每章末尾自测题检验掌握程度。

## 页面组件
1. 大标题+简介
2. 课程模块说明卡片（概率论 / 数理统计）
3. 8个章节跳转卡片，点击跳转到对应pages/chapterN.html
4. 使用说明

## 无可视化图表，无LaTeX公式
> 输出完整index.html，包含全局侧边导航组件，可供后续所有chapter页面复用布局。

---

# chapter1｜随机事件及其概率
## 本章总述
> 核心：随机试验、样本空间、事件运算、概率公理、古典概型、条件概率、全概率、贝叶斯、独立与互斥。
> 应试重点：德摩根定律、加法公式、条件概率、全概率、贝叶斯；区分独立与互斥。

### 卡片1：韦恩图 — 事件的运算
**知识点文本**
随机事件可以做交、并、补、差运算；德摩根定律是事件运算重要恒等式。
互斥：$\(\overline A\)\(\overline B\)=\emptyset$，\(\overline A\)、\(\overline B\)不可能同时发生。
> ⚠️重要误区：互斥 ≠ 相互独立。

**LaTeX公式**
```latex
\(\overline A\)\cup \(\overline B\),\; \(\overline A\)\cap \(\overline B\),\; \overline{\(\overline A\)},\; \(\overline A\)-\(\overline B\)
\overline{\(\overline A\)\cup \(\overline B\)}=\overline \(\overline A\) \cap \overline \(\overline B\)
\overline{\(\overline A\)\cap \(\overline B\)}=\overline \(\overline A\) \cup \overline \(\overline B\)
```

**可视化规格**
- 图表：Venn韦恩图，两个集合\(\overline A\)、\(\overline B\)
- 控件：
  复选框：$\(\overline A\)\cup \(\overline B\)$、$\(\overline A\)\cap \(\overline B\)$、$\overline \(\overline A\)$、$\overline \(\overline B\)$、$\overline{\(\overline A\)\cup \(\overline B\)}$
  按钮：【互斥模式】【重置】
- 交互：勾选选项，图形高亮对应区域；切换互斥模式，两集合无交集；演示德摩根定律两边区域完全等价。

**应试提示**
德摩根定律填空选择高频；口诀：杠进去，并变交，交变并。互斥不等于独立，极易混淆。

### 卡片2：频率与概率｜抛硬币模拟
**知识点文本**
频率：重复试验中事件发生的次数/总试验次数，随试验改变。
概率：事件固有的理论值；当试验次数很大时，频率依概率收敛到概率。

无公式

**可视化规格**
- 图表：折线图X：试验次数n；Y：正面出现频率
- 控件：滑块 n：1 ~ 10000；按钮【开始模拟】【重置】
- 交互：n较小时频率剧烈震荡；n增大，曲线向理论概率0.5收敛；同时显示当前模拟正面次数。

**应试提示**
频率是实验结果，概率是理论常数，不要混淆概念。

### 卡片3：条件概率、贝叶斯（疾病检测案例）
**知识点文本**
条件概率：在\(\overline A\)发生前提下\(\overline B\)发生的概率；贝叶斯由先验概率求后验概率。
很多人直觉高估后验概率，面积可视化帮助纠正直觉错误。

**LaTeX公式**
```latex
P(\(\overline B\)|\(\overline A\))=\frac{P(\(\overline A\)\(\overline B\))}{P(\(\overline A\))},\; P(\(\overline A\))>0
P(\(\overline B\))=\sum_i P(\(\overline A\)_i)P(\(\overline B\)|\(\overline A\)_i)
P(\(\overline A\)_j|\(\overline B\))=\frac{P(\(\overline A\)_j)P(\(\overline B\)|\(\overline A\)_j)}{\sum_i P(\(\overline A\)_i)P(\(\overline B\)|\(\overline A\)_i)}
```

**可视化规格**
- 图表：矩形面积图，面积代表概率占比
- 控件：滑块：先验概率P(\(\overline A\))，真阳性率，假阳性率；【重置】
- 交互：拖动滑块实时计算并展示后验概率P(\(\overline A\)|\(\overline B\))；图形面积同步更新。

**应试提示**
全概率、贝叶斯为考试大题考点；注意条件概率分母不能丢。

**页面底部自测题（单选3道）**
1. $\overline{\(\overline A\)\cup \(\overline B\)}=$？
2. \(\overline A\)、\(\overline B\)互斥，则\(\overline A\)、\(\overline B\)是否一定独立？
3. 条件概率P(\(\overline B\)|\(\overline A\))分母是什么？

---

# chapter2｜一维随机变量及其分布
## 本章总述
> 核心：离散/连续随机变量；分布律、概率密度、分布函数F(x)；六大常用分布。
> 应试重点：分布函数性质；6大分布记号、期望方差；正态标准化；连续变量概率等于密度下面积。

### 卡片1：离散分布交互式（0‑1、二项、泊松、几何）
**知识点文本**
离散随机变量取有限/可列无穷个值；分布律描述各个取值的概率。

**LaTeX公式**
```latex
P(X=x_k)=p_k,\quad \sum p_k =1
\(\overline B\)(n,p),\; P(\lambda),\; G(p)
```

**可视化规格**
- 图表：柱状图
- 控件：下拉切换分布；对应参数滑块(n,p,λ)；重置
- 交互：修改参数柱子高度实时变化；面板展示该分布E(X),D(X)。

**应试提示**
二项、泊松期望方差直接背，考试不要现场推导。

### 卡片2：连续分布（均匀、指数、正态）密度与分布函数
**知识点文本**
连续随机变量用概率密度$\(f(x)\)$描述；概率是曲线下面积；分布函数 $F(x)=P(X\le x)$。

**LaTeX公式**
```latex
F(x)=P(X\le x),\quad P(a< X \le b)=\int_{a}^{b} \(f(x)\)dx
U(a,b),E(\lambda),N(\mu,\sigma^2)
```

**可视化规格**
- 双图并排：上方概率密度\(f(x)\)曲线；下方分布函数F(x)
- 控件：下拉切换分布；参数滑块$\mu,\sigma,\lambda,a,b$；鼠标点击X轴，填充阴影面积显示$P(X\le x)$；重置。
- 交互：参数改变曲线实时更新；指数分布演示无记忆性区域高亮。

**应试提示**
连续型随机变量单点概率$P(X=x_0)=0$。

### 卡片3：正态分布标准化
**知识点文本**
任意正态可以变换为标准正态 $Z \sim N(0,1)$，查表计算概率。

**LaTeX公式**
```latex
Z=\frac{X-\mu}{\sigma}
```

**可视化规格**
- 双图：原图$N(\mu,\sigma^2)$；标准正态N(0,1)
- 控件：$\mu$、$\sigma$滑块；输入X值；重置
- 交互：输入X，自动计算Z值，两张图同步标记对应位置。

**应试提示**
正态标准化期末考试必考。

### 卡片4：分布函数性质演示
**知识点文本**
F(x)单调不减；$0\le F(x)\le1$；$F(-\infty)=0,\;F(+\infty)=1$；离散F是阶梯函数，连续F平滑。

无公式

**可视化规格**
- 控件：切换离散/连续总体；观察F(x)形态变化。

**应试提示**
分布函数四条性质，选择题高频。

**底部自测3道单选**

---

# chapter3｜二维随机变量及其分布
## 本章总述
> 核心：联合分布、边缘分布、条件分布；二维正态；随机变量独立性。
> 应试重点：会求边缘分布；独立判定；二维正态相关系数$\rho$含义。

### 卡片1：二维离散联合分布热力图
**知识点文本**
联合分布$P(X=x_i,Y=y_j)$；对行求和得到Y边缘分布；对列求和得到X边缘分布。
> 误区：仅知道边缘分布，不能推出联合分布。

**LaTeX公式**
```latex
P(X=x_i)=\sum_j P(X=x_i,Y=y_j)
P(Y=y_j)=\sum_i P(X=x_i,Y=y_j)
```

**可视化规格**
- 图表：热力表格，颜色深浅代表联合概率大小
- 按钮：【计算边缘分布】【重置】
- 交互：点击按钮，行、列求和结果展示在表格外侧。

**应试提示**
大题经常考由联合求边缘。

### 卡片2：二维正态3D曲面 & 等高线
**知识点文本**
二维连续联合密度，可用三维曲面表示；相关系数$\rho$刻画线性关联。

**LaTeX公式**
```latex
(X,Y)\sim N(\mu_1,\mu_2,\sigma_1^2,\sigma_2^2,\rho)
```

**可视化规格**
- Plotly 3D曲面图 + 2D等高线子图
- 控件：滑块 $\mu_1,\mu_2,\sigma_1,\sigma_2,\rho(-1\sim1)$；重置
- 交互：改变$\rho$观察曲面、等高线形态；$\rho=0$对应独立。

**应试提示**
二维正态：$\rho=0 \iff X,Y$独立；普通分布不成立。

### 卡片3：二维概率积分区域
**知识点文本**
$P\{(X,Y)\in D\}$ 是联合密度在区域D上二重积分。

**可视化规格**
- 等高线图；可框选矩形区域D；区域高亮，输出计算得到概率数值。

**应试提示**
考试二重积分求概率，注意积分上下限。

### 卡片4：二维独立判定演示
**知识点文本**
连续：$f(x,y)=f_X(x)f_Y(y)$；离散：$P(X=x_i,Y=y_j)=P(X=x_i)P(Y=y_j)$。

可视化：切换独立/不独立样本，对比联合与边缘乘积。

底部自测单选3道。

---

# chapter4｜随机变量的数字特征
## 本章总述
> 核心：期望、方差、协方差、相关系数；性质公式。
> 应试重点：期望方差性质；协方差；相关系数含义；$\rho=0$不代表独立。

### 卡片1：期望 — 概率重心
**知识点文本**
期望是加权平均，物理上相当于概率分布的重心，不一定是随机变量可以取到的值。

**LaTeX公式**
```latex
E(X)=\sum x_i p_i \quad(离散)
E(X)=\int_{-\infty}^{+\infty} x \(f(x)\) dx \quad(连续)
E(aX+b)=aE(X)+b
```

**可视化规格**
- 离散柱状概率图；图上标记竖线代表期望重心；改变分布参数，重心移动。

**应试提示**
期望线性性质永远成立，不要求独立。

### 卡片2：方差 — 离散程度
**知识点文本**
方差衡量随机变量取值波动大小。

**LaTeX公式**
```latex
D(X)=E\left[(X-E(X))^2\right]
\(D(aX+b)\)=a^2D(X)
```

**可视化规格**
- 正态曲线，固定期望，调节$\sigma$，观察图形胖瘦变化。

**应试提示**
$\(D(aX+b)\)$系数是$a^2$，很多学生写成a，高频错误。

### 卡片3：协方差、相关系数散点模拟
**知识点文本**
协方差衡量X,Y协同变化；相关系数$\rho\in[-1,1]$只刻画线性相关。
> $\rho=0$：不线性相关，**不代表相互独立**。

**LaTeX公式**
```latex
Cov(X,Y)=E[(X-EX)(Y-EY)]
\rho_{XY}=\frac{Cov(X,Y)}{\sqrt{D(X)}\sqrt{D(Y)}}
```

**可视化规格**
- 散点图；滑块调节$\rho$；按钮生成样本；实时输出Cov、\(\rho\)数值。
- 演示：非线性样本，$\rho\approx0$但是不独立。

**应试提示**
填空选择题高频考点。

底部自测单选3道。

---

# chapter5｜大数定律及中心极限定理
## 本章总述
> 大数定律：样本均值收敛于理论期望；
> 中心极限定理CLT：大量独立同分布样本均值近似服从正态分布，**原始总体不需要正态**。
> 应试重点：中心极限定理大题。

### 卡片1：大数定律模拟
**知识点文本**
n增大，样本算术平均值趋近总体数学期望。

可视化规格
- 控件：下拉选择总体（均匀/指数/二项）；n滑块；开始模拟
- 图表：折线图，X样本量n，Y样本均值；对比红色水平线（理论期望）。

### 卡片2：中心极限定理【王牌可视化】
**知识点文本**
无论原来总体是什么分布，独立同分布，n足够大，样本均值近似正态。

**LaTeX公式**
```latex
\frac{\overline X -\mu}{\sigma/\sqrt{n}} \stackrel{近似}{\sim} N(0,1)
```

**可视化规格**
- 图表：样本均值直方图 + 叠加理论正态曲线
- 控件：下拉选择原始总体：均匀 / 指数 / 二项；样本容量n滑块；模拟次数滑块；【开始模拟】【重置】
- 交互：n很小直方图形状怪异；n增大，直方图逐步逼近钟形正态曲线。

**应试提示**
期末考试大题高频；记住：是样本均值趋近正态，不是原始数据。

底部自测单选3道。

---

# chapter6｜样本及抽样分布
## 本章总述
> 样本、统计量；样本均值、样本方差；三大抽样分布$\chi^2、t、F$；分位数。
> 应试重点：三大分布形态、自由度；上\(\alpha\)分位数含义。

### 卡片1：样本生成器，总体vs样本
**知识点文本**
总体参数是常数；样本统计量是随机变量，抽样会发生变化。

可视化：给定总体，抽取样本；输出样本均值$\bar X$，样本方差$S^2$，对比总体E(X),D(X)。

### 卡片2：三大抽样分布交互式曲线
**知识点文本**
$\chi^2(n)$；$\(t(n)\)$；$F(n_1,n_2)$；自由度改变分布形态。

**LaTeX公式**
```latex
\chi^2(n),\ \(t(n)\),\ F(n_1,n_2)
```

**可视化规格**
- 切换分布；自由度滑块；输入上侧\(\alpha\)；
- 图表：概率密度曲线；尾部阴影标出上\(\alpha\)分位数，标出临界值横坐标。

交互：
1.$\chi^2$：自由度增大，右偏逐渐趋向正态
2.t分布：自由度越小尾部越厚，大自由度逼近标准正态
3.F分布恒右偏。

**应试提示**
分位数是后续参数估计、假设检验查表基础。

### 卡片3：样本均值抽样分布模拟
重复抽样，画出$\bar X$直方图，印证抽样分布结论。

底部自测单选3道。

---

# chapter7｜参数估计
## 本章总述
> 点估计（矩估计、极大似然估计）；无偏性；区间估计、置信区间。
> 应试重点：置信区间含义；正态总体区间估计。

### 卡片1：点估计模拟，有偏与无偏
**知识点文本**
点估计：用样本算出一个数值作为总体参数估计；无偏：估计量的期望等于真实参数。

可视化规格
- 红色竖线固定代表总体真实参数；重复抽样，每一组样本输出一个估计点，散点画在图上。
- 切换无偏估计 / 有偏估计，观察点分布：无偏点平均围绕真值；有偏整体偏移。

**应试提示**
无偏是估计量期望等于真值，不是每一次估计都等于真值。

### 卡片2：置信区间模拟（教学神器）
**知识点文本**
95%置信区间含义：大量重复抽样构造区间，大约95%区间包含真实总体参数，**不是参数95%落在某一个固定区间**。

可视化规格
- 红色竖线：真实固定参数；
- 绘制多条水平线段代表每一组样本得到置信区间；
- 控件：置信水平下拉90% /95% /99%；模拟组数；重置。
交互：置信水平越高，区间线段越宽；少数线段不穿过红色真值线。

**应试提示**
置信区间概念选择题高频坑。

### 卡片3：正态总体区间估计对比
切换已知\(\sigma\)（Z分布）、未知\(\sigma\)（t分布），观察区间变化。

底部自测单选3道。

---

# chapter8｜假设检验
## 本章总述
> 原假设$H_0$，备择假设$H_1$；显著性水平$\alpha$；拒绝域；p‑value；两类错误。
> 应试重点：拒绝域、p值与\(\alpha\)比较；一类错误二类错误；单侧双侧检验。

### 卡片1：拒绝域可视化
**知识点文本**
给定显著性水平$\alpha$，分布尾部为拒绝域；统计量落入拒绝域，则拒绝原假设。

可视化规格
- 图表：Z/t分布曲线；尾部涂色代表拒绝域；标记临界值；
- 控件：切换单侧/双侧检验；\(\alpha\)滑块；输入观测统计量；重置。
交互：判断统计量是否落在拒绝域，输出文字结论：拒绝$H_0$ / 不拒绝$H_0$。

### 卡片2：P‑value可视化
**知识点文本**
p值：原假设成立时观测到当前以及更极端样本的概率；若$p<\alpha$拒绝$H_0$。

可视化：在图上标出p对应的尾部面积。

### 卡片3：两类错误模拟
第一类错误（弃真）：$H_0$为真，拒绝$H_0$；概率$\alpha$。
第二类错误（取伪）：$H_1$为真，接受$H_0$。
> 二者不能同时减小。

可视化规格
模拟多次检验，统计两类错误发生次数；调节\(\alpha\)，观察一类错误降低，二类错误上升。

### 卡片4：单侧检验 vs 双侧检验一键切换
直观对比拒绝域位置。

底部自测单选3道。

---

# \(\overline AI\)生成提示词使用守则（写在spec末尾）
1. ❌禁止一次性把全部8个章节丢给\(\overline AI\)，代码过长，bug爆炸。
2. ✅生成顺序：
   1）复制【全局UI&技术规范】+【首页index.html规格】→生成 index.html，本地Live Server跑通，确认侧边导航正常。
   2）依次生成 chapter1 ~ chapter8：每次复制【全局UI】+【侧边导航】+**单章完整规格**，生成单页面。
   3）每生成一章，本地调试，滑块、按钮、图表、公式全部测试，修复bug，再生成下一章。
3. \(\overline B\)ug修复提示词模板：
```
当前网页出现bug：【描述现象】
基于上面完整代码修复该问题，输出完整可运行的全部HTML。
```
4. 部署：全部页面完成后上传GitHub，开启GitHub Pages。

---
